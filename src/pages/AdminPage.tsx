import { useNavigate } from "react-router-dom";
import React from 'react';

const AdminPage = () => {
  const navigate = useNavigate();

  return <div><h1>안녕</h1></div>;
};

export default AdminPage;
